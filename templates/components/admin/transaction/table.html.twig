<div class="min-h-[650px] rounded-lg border border-gray-200 mt-6 overflow-x-auto">
    <div class="flex bg-gray-100 h-11 rounded-t-lg items-center justify-start">
        <div class="px-6 min-w-16">
            <input type="checkbox"
                   onclick="((source) => {
                       let checkboxes = document.getElementsByClassName('checkbox-all');
                       for (let i = 0; i < checkboxes.length; i++) {
                           let checkbox = checkboxes[i];
                           checkbox.checked = source.checked
                       }
                   }) (this)"
            >
        </div>
        <div class="px-6 min-w-[110px]">
            <p class="text-sm text-slate-700">ID</p>
        </div>
        <div class="px-6 min-w-[170px]">
            <p class="text-sm text-slate-700">Entreprise</p>
        </div>
        <div class="px-6 min-w-[210px]">
            <p class="text-sm text-slate-700">Client</p>
        </div>
        <div class="px-6 min-w-[200px]">
            <p class="text-sm text-slate-700">Moyen de paiement</p>
        </div>
        <div class="px-6 min-w-[180px]">
            <p class="text-sm text-slate-700">Montant</p>
        </div>
        <div class="px-6 min-w-[180px]">
            <p class="text-sm text-slate-700">État</p>
        </div>
        <div class="px-6 min-w-[180px]">
            <p class="text-sm text-slate-700">Date</p>
        </div>

    </div>
    {% block content %}

    {% endblock %}
</div>